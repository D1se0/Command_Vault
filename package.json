{
  "name": "command-vault",
  "version": "2.0.0",
  "private": true,
  "description": "Command Vault - Base de datos local de comandos para pentesting y red team",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "seed:rebuild": "python3 server/data/seed/build_seed.py"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
